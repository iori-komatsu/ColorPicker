<Window x:Class="ColorPicker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorPicker"
        mc:Ignorable="d"
        Title="Color Picker" Height="500" Width="500">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <local:HsvControl Grid.Row="0" Grid.Column="0" x:Name="hsvControl" PropertyChanged="hsvControl_PropertyChanged"></local:HsvControl>
        <Border Name="colorDisplay" Grid.Row="1" Grid.Column="0" Margin="10">
            <TextBlock Name="colorDisplayTextBox" TextAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="20"/>
        </Border>
    </Grid>
</Window>
